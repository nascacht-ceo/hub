# .github/actions/setup/action.yml
name: 'Setup'
description: 'Prepares SDK, Updates Packages, and Restores'
runs:
  using: "composite"
  steps:
    - name: Setup netcore 10.0.x
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Start LocalStack
      shell: bash
      run: |
        docker run -d --rm \
          --name localstack_main \
          -p 4566:4566 -p 4571:4571 \
          -e SERVICES=lambda,s3,secretsmanager,dynamodb \
          -e LAMBDA_RUNTIME_ENVIRONMENT_TIMEOUT=120 \
          -e DEBUG=1 \
          -v /var/run/docker.sock:/var/run/docker.sock \
          localstack/localstack

    - name: Wait for LocalStack
      shell: bash
      run: |
        # DISABLE GitHub's "Kill on Error" settings for this step
        set +e
        set +o pipefail
        echo "Waiting for LocalStack to be ready..."
        # Increase retries for CI stability
        MAX_RETRIES=60
        COUNT=0
        # By using 'while !', bash treats the curl failure as a valid 
        # result for the loop, preventing 'set -e' from killing the script.
        while ! curl -s --fail http://localhost:4566/_localstack/health > /dev/null; do
          if [ $COUNT -eq $MAX_RETRIES ]; then
            echo "Timed out waiting for LocalStack"
            docker logs localstack_main
            exit 1
          fi
          echo "Still waiting... ($COUNT/$MAX_RETRIES)"
          sleep 2
          ((COUNT++))
        done
        echo "LocalStack is ready!"

    - name: Nuget cache
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/global.json', '**/*.csproj') }}

    - name: Inject All Secrets
      shell: bash
      # This pulls all secrets into the current job's environment
      # so your .NET code can see them as Environment Variables.
      run: |
        echo '${{ toJSON(secrets) }}' | jq -r 'to_entries[] | "\(.key)=\(.value)"' >> $GITHUB_ENV

    - name: Restore
      shell: bash
      run: |
        dotnet restore